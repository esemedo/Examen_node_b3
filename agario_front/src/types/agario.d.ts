type OffsetObject = {
    x:number,
    y:number, 
}

type Player = {
    color: string, 
    point : number
    x: number, 
    y : number ,
    id: string,

}

type AllPlayer = {
    id: string,
    info: Player
}